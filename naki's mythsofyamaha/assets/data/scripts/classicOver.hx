

function postCreate() {
    // trace("GameOvered");

    lossSFX.volume = 0.001;

    // FlxG.sound.music.pitch = 0.4;

    // new FlxTimer().start(1, (_) -> {
    //     FlxG.sound.music.pitch = 0.4;
    // });
}